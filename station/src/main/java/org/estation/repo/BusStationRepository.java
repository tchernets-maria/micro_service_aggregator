package org.estation.repo;

import org.estation.models.BusStation;
import org.springframework.data.repository.CrudRepository;

public interface BusStationRepository extends CrudRepository<BusStation, Long> {
}
